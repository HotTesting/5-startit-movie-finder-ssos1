import { browser, $, $$, element, by} from 'protractor'

describe('Search ', async function(){

    xit('by exisiting name, should show first movie with complete name match', async function(){

    })

    xit('results(all of them) should contain serach request'), async function(){

    }

    xit('result should be empty, after request for nonexistent movie'), async function(){

    }
})